arange

zeros

ones

```python
arange
zeros
ones
ndarry:
    a = numpy.array([1,2,3])
    shape
    dtype
    astype("")
    size
    ndim
    itemsize
    nbytes
    real
    imag
    T
    
# numpy的基础数据类型:
布尔
有符号整数
无符号整数
浮点
复数
字符串
日期  datetime64 M8[Y] M8[D] ...

# 自定义复数类型
a = numpy.array(data,dtype="")

# 维度操作
# 1.视图变维
reshape() ravel()
# 2.复制变维
fattern() copy()
# 3.就地变维
shape

# 数组切片
a = numpy.arange(1,19).shape(3,2,3)
a[0:,0:1,0:2]

# 数组的掩码操作


# 多维数组的组合与拆分
# 水平方向
# 组合
# 拆分

# 垂直方向

# 深度方向

# 通用
concatenate()
split
```

